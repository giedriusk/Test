#include <stdio.h>
#include <ctype.h>
#include <string.h>
#include <stdlib.h>

accum(const char *source) {
  int l = strlen(source);
  int i = 0;
  char *result;
  char *tmp = (char *)malloc(l * sizeof(char));
//  char *prev = "";
  strcpy(tmp, source);
  while(*tmp != '\0') {
//    result[i] += toupper(*tmp);
//    prev = tmp;
    printf("%s\r\n", tmp);
    tmp++;
  }
  
  return result;
}

int main() {
  char *source = "Test";
  printf("Result: %s\r\n", accum(source));
  return 0;
}
